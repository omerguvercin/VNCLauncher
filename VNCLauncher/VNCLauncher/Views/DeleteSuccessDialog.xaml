<Window x:Class="VNCLauncher.Views.DeleteSuccessDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:VNCLauncher.Views"
        mc:Ignorable="d"
        Title="Silme Başarılı" Height="200" Width="400" WindowStartupLocation="CenterOwner" 
        ResizeMode="NoResize" ShowInTaskbar="False" Background="#F5F5F5">
    
    <Window.Resources>
        <!-- Başarı Buton Stili -->
        <Style x:Key="SuccessButton" TargetType="Button">
            <Setter Property="Height" Value="36"/>
            <Setter Property="Padding" Value="15,0"/>
            <Setter Property="Background" Value="#2ECC71"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="4" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                BorderBrush="{TemplateBinding BorderBrush}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#27AE60"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#1E8449"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    
    <Border BorderBrush="#DDDDDD" BorderThickness="1" Background="White" CornerRadius="8" Margin="15">
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <!-- Başlık ve İkon -->
            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,15">
                <Path Width="32" Height="32" Margin="0,0,15,0" Fill="#2ECC71" 
                      Data="M21,7L9,19L3.5,13.5L4.91,12.09L9,16.17L19.59,5.59L21,7Z" 
                      Stretch="Uniform" VerticalAlignment="Center"/>
                <TextBlock Text="İşlem Başarılı" FontWeight="Bold" FontSize="20" Foreground="#333333" VerticalAlignment="Center"/>
            </StackPanel>
            
            <!-- Mesaj -->
            <TextBlock x:Name="txtMessage" Grid.Row="1" 
                     Text="Bağlantı başarıyla silindi." 
                     TextWrapping="Wrap" Margin="0,10,0,10" FontSize="14" Foreground="#555555"/>
            
            <!-- Butonlar -->
            <Grid Grid.Row="2" Margin="0,15,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <Button x:Name="btnOk" Grid.Column="1" Width="100" 
                        Style="{StaticResource SuccessButton}" Click="BtnOk_Click">
                    <StackPanel Orientation="Horizontal">
                        <Path Width="16" Height="16" Margin="0,0,5,0" Fill="White" 
                              Data="M21,7L9,19L3.5,13.5L4.91,12.09L9,16.17L19.59,5.59L21,7Z" 
                              Stretch="Uniform" />
                        <TextBlock Text="Tamam"/>
                    </StackPanel>
                </Button>
            </Grid>
        </Grid>
    </Border>
</Window> 